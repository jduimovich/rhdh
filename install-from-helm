SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )" 
source $SCRIPTDIR/verify-dependencies

NS=rhdh 
oc new-project $NS
echo "Install from Helm"
#helm install rhtap-dance/dance --generate-name --namespace $NS --values values.yaml
helm install rhtap-dance/dance --generate-name --namespace $NS --values values.yaml